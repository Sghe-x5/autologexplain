# Backend env
AI_API_KEY={{ ai_api_key }}
AI_MODEL={{ ai_model }}
REDIS_HOST={{ redis_host }}
REDIS_PORT={{ redis_port }}
REDIS_DB={{ redis_db }}
REDIS_PASSWORD={{ redis_password | default('') }}
CELERY_BROKER_URL={{ celery_broker_url }}
CELERY_BACKEND_URL={{ celery_backend_url }}
CLICKHOUSE_HOST={{ clickhouse_host }}
CLICKHOUSE_PORT={{ clickhouse_port }}
CLICKHOUSE_USER={{ clickhouse_user }}
CLICKHOUSE_PASSWORD={{ clickhouse_password }}
CLICKHOUSE_DB={{ clickhouse_db }}
CLICKHOUSE_TABLE={{ clickhouse_table }}
MAX_PAGE_SIZE={{ max_page_size }}
MAX_HARD_LIMIT={{ max_hard_limit }}
TOKEN_SECRET={{ token_secret }}
TOKEN_TTL_SECONDS={{ token_ttl_seconds }}
CHAT_TTL_SECONDS={{ chat_ttl_seconds }}
INCIDENT_DETECTOR_INTERVAL_MINUTES={{ incident_detector_interval_minutes | default(2) }}
INCIDENT_DETECTOR_LOOKBACK_MINUTES={{ incident_detector_lookback_minutes | default(360) }}
INCIDENT_DETECTOR_MAX_LOGS={{ incident_detector_max_logs | default(100000) }}
INCIDENT_ANOMALY_THRESHOLD={{ incident_anomaly_threshold | default(3.5) }}
INCIDENT_SLO_TARGET={{ incident_slo_target | default(0.995) }}
INCIDENT_CORRELATOR_INTERVAL_MINUTES={{ incident_correlator_interval_minutes | default(1) }}
INCIDENT_CORRELATOR_LOOKBACK_MINUTES={{ incident_correlator_lookback_minutes | default(120) }}
INCIDENT_CORRELATOR_MAX_CANDIDATES={{ incident_correlator_max_candidates | default(500) }}
INCIDENT_CORRELATION_WINDOW_MINUTES={{ incident_correlation_window_minutes | default(30) }}
INCIDENT_REOPEN_WINDOW_MINUTES={{ incident_reopen_window_minutes | default(360) }}
INCIDENT_RCA_INTERVAL_MINUTES={{ incident_rca_interval_minutes | default(3) }}
INCIDENT_RCA_MAX_INCIDENTS={{ incident_rca_max_incidents | default(200) }}
INCIDENT_RCA_TRACE_LOOKBACK_MINUTES={{ incident_rca_trace_lookback_minutes | default(180) }}
INCIDENT_JOB_LOCK_TTL_SECONDS={{ incident_job_lock_ttl_seconds | default(240) }}
YC_FOLDER_ID={{ folder_id }}
YC_IAM_TOKEN={{ iam_token }}
YC_API_KEY={{ ai_api_key }}
CLICKHOUSE_DATABASE={{ clickhouse_db }}
OPENROUTER_API_KEY={{ openrouter_api_key }}
YC_KEY_ID={{ yc_key_id }}
YC_SERVICE_ACCOUNT_ID={{ yc_service_account_id }}
YC_PRIVATE_KEY={{ yc_private_key }}
