### **LogExplainUI**

- `log-explain-modal` — корневой контейнер модалки
- `log-explain-header` — шапка модалки
- `header-left` — левая часть шапки
- `assistant-icon` — иконка ассистента
- `assistant-title` — заголовок
- `assistant-description` — описание под заголовком
- `close-modal-button` — кнопка закрытия
- `reset-log-button` — кнопка «Сбросить и задать новые параметры»
- `separator` — разделитель
- `filters-loader` — индикатор загрузки фильтров
- `log-explain-form-section` — секция формы параметров
- `chat-with-ai-section` — секция чата с AI

---

### **DatePicker**

- `date-picker` — контейнер
- `date-picker-label` — подпись
- `date-picker-wrapper` — обёртка поля
- `date-input` — инпут даты
- `open-calendar-trigger` — триггер открытия календаря
- `open-calendar-button` — кнопка с иконкой календаря
- `calendar-popover` — поповер календаря
- `calendar-wrapper` — контейнер календаря
- `calendar` — сам календарь
- `calendar-footer` — футер с кнопками
- `clear-date-button` — кнопка «Очистить»
- `set-today-button` — кнопка «Сегодня»
- `time-picker-wrapper` — обёртка таймпикера
- `time-picker` — сам компонент таймпикера

---

### **TimePicker**

- `time-picker` — контейнер
- `time-picker-display` — блок текущего времени
- `time-picker-hours` — блок часов
- `time-picker-hours-value` — текущее значение часов
- `increase-hours-button` — кнопка ↑ для часов
- `decrease-hours-button` — кнопка ↓ для часов
- `time-picker-separator` — двоеточие `:`
- `time-picker-minutes` — блок минут
- `time-picker-minutes-value` — текущее значение минут
- `increase-minutes-button` — кнопка ↑ для минут
- `decrease-minutes-button` — кнопка ↓ для минут
- `time-picker-scrolls` — обёртка скроллов
- `hour-scroll` — скролл для часов
- `minute-scroll` — скролл для минут
- `hour-option-{h}` — элемент выбора часа
- `minute-option-{m}` — элемент выбора минут

---

### **App**

- `app-root` — контейнер приложения
- `modal-backdrop` — затемнение при открытой модалке
- `app-background` — картинка фона
- `log-explain-ui` — вставленный портал с модалкой
- `log-explain-button` — кнопка вызова ассистента

---

### **ChatWithAI**

- `chat-with-ai` — контейнер чата
- `chat-header` — шапка чата
- `chat-messages-scroll` — область прокрутки сообщений
- `chat-message-user` — сообщение от пользователя
- `chat-message-assistant` — сообщение от ассистента
- `chat-input-wrapper` — обёртка ввода
- `chat-input` — поле ввода
- `chat-send-button` — кнопка отправки

---

### **LogExplainForm**

- `log-explain-form` — форма
- `product-select` — селект продукта
- `product-select-trigger` — триггер продукта
- `product-select-options` — список продуктов
- `product-option-{product}` — пункт продукта
- `service-select` — селект сервиса
- `service-select-trigger` — триггер сервиса
- `service-select-options` — список сервисов
- `service-option-{service}` — пункт сервиса
- `environment-select` — селект окружения
- `environment-select-trigger` — триггер окружения
- `environment-select-options` — список окружений
- `environment-option-{env}` — пункт окружения
- `period-fields` — блок выбора периода
- `start-time-picker` — пикер начала
- `end-time-picker` — пикер окончания
- `comment-field` — блок комментария
- `comment-textarea` — поле комментария
- `comment-counter` — счётчик символов
- `form-separator` — разделитель
- `analyze-submit-button` — кнопка сабмита

---

### **FormInput**

- `form-input` — универсальный инпут (дефолт, можно переопределить)

---

### **LogExplainBtn**

- `open-modal-button` — сама кнопка
- `open-modal-icon` — иконка
- `open-modal-label` — скрытая подпись
